<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.MemberMapper">
<resultMap type="org.zerock.domain.MemberVO" id="memberMap">
<id property="userid" column="userid"/>
<result property="userid" column="userid"/>
<result property="userpw" column="userpw"/>
<result property="userName" column="username"/>
<result property="regDate" column="regDate"/>
<result property="updateDate" column="updateDate"/>
<collection property="authList" resultMap="authMap">
</collection>
</resultMap>

<resultMap type="org.zerock.domain.AuthVO" id="authMap">
<result property="userid" column="userid"/>
<result property="auth" column="auth"/>

</resultMap>

<select id="read" resultMap="memberMap">
SELECT mem.userid, userpw, username, enabled, regdate, updatedate, auth, auth.userid
		FROM ynews_member mem 
		LEFT OUTER JOIN ynews_member_auth auth
		on mem.userid = auth.userid
		WHERE mem.userid = #{userid}

</select>

<insert id="insert">
insert into ynews_member(userid,userpw,username,useremail)
values(#{userid},#{userpw},#{username},#{useremail})

</insert>

<update id="update">
update ynews_member set userpw = #{userpw}, username =#{username}, useremail=#{useremail} where userid = #{userid} 

</update>

<delete id="delete">
delete from ynews_member where userid= #{userid}
</delete>




</mapper>